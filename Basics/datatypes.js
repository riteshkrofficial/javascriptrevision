//let us see the datatypes here 

let isAvailabe = true;
console.log(isAvailabe);

// null-> it means no value that is suppose you do not know about a particular things then it null value can be used in its place 
// undefined -> it means you declared the data its other thing that you do not know the measuring scale but data is their

let state =null;
//important 
console.log(typeof null);   //null is an object
console.log(typeof undefined); // its is undefined type 
